let nome = prompt ('Digite Seu Nome')
let idade = prompt ('Digite Sua Idade')

if ( nome === 'Jadson' || nome === 'jadson') {
    document.write (`Ola ${nome}, Seja Bem Vindo`)
}

if (idade === 24) {
    console.log (idade)
}